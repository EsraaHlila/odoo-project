<odoo>
    <!-- Action to open subscriptions -->
    <record id="action_unique_subscription" model="ir.actions.act_window">
        <field name="name">Subscriptions</field>
        <field name="res_model">unique.subscription</field>
        <field name="view_mode">list,form,kanban,graph</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first subscription</p>
        </field>
    </record>

    <!-- Server Action for Sync -->
    <!--<record id="action_sync_all_subscriptions" model="ir.actions.server">
        <field name="name">Sync All Subscriptions</field>
        <field name="model_id" ref="model_unique_subscription"/>
        <field name="binding_model_id" ref="model_unique_subscription"/>
        <field name="state">code</field>
        <field name="code">
            model.action_sync_all_subscriptions()
        </field>
    </record> -->
</odoo>
