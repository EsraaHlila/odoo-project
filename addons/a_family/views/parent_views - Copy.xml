<odoo>
    <!-- Tree View -->
    <record id="unique_parent_tree_view" model="ir.ui.view">
        <field name="name">unique.parent.tree</field>
        <field name="model">unique.parent</field>
        <field name="arch" type="xml">
            <list>
				<field name="first_name"/>
				<field name="last_name"/>
				<field name="birth_date"/>
				<field name="address"/>
				<field name="children_count"/>
				<field name="gender"/>
				<field name="activee"/>
				<field name="child_id"/>
				<field name="code"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="unique_parent_form_view" model="ir.ui.view">
        <field name="name">unique.parent.form</field>
        <field name="model">unique.parent</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
						<field name="first_name"/>
						<field name="last_name"/>
						<field name="birth_date"/>
						<field name="address"/>
						<field name="children_count"/>
						<field name="gender"/>
						<field name="activee"/>
						<field name="child_id"/>
						<field name="code"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="unique_parent_action" model="ir.actions.act_window">
        <field name="name">Parents</field>
        <field name="res_model">unique.parent</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first parent</p>
        </field>
    </record>
</odoo>